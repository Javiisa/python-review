#Los arreglos son una estructura de datos muy utilizada
# Ordena sus elementos en base a su posiciòn o ìndice

#Arreglo de strings
simple_array = ['1','2','3','4','5']

#Arreglo de nùmeros
other_array = [1,2,3,4,5]

print(simple_array[1])
print(other_array[1])
#Podemos saber el largo del arreglo con la funciòn len

print(len(simple_array))
print(len(other_array))
